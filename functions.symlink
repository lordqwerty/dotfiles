function chpwd() {
emulate -L zsh

if [ -d ".git" ]; then
echo "$bg[red]$fg[white] Git status $reset_color"
git status
echo ''
fi

if [ -e "todo.txt" ]; then
echo "$bg[red]$fg[white] TODO list $reset_color"
cat todo.txt
echo ''
fi
}